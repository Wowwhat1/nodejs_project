<div class="container">
    <div class="row">
        <div class="col-md-6">
            <img src="{{toy.image}}" alt="{{toy.name}}" class="img-fluid">
        </div>
        <div class="col-md-6">
            <h2>{{toy.name}}</h2>
            <p>{{toy.description}}</p>
        </div>
    </div>
</div>